# Training reports

Colab reports persist under `colab/` in this Drive project. Every completed run
creates a timestamped PDF, a stable `latest` PDF, and a JSON manifest.

The Kaggle notebook writes directly to `/kaggle/working/ugvnet/reports` inside
Kaggle, so no repository-side Kaggle folder is needed.

Generated report files are ignored by Git.
